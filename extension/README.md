# Connoction Extension

## Build
```bash
cd extension
npm install
npm run build
```

## Load in Chrome
- Open chrome://extensions
- Enable Developer Mode
- Load unpacked → select the `extension/` folder
- Pin the extension and click the icon on a LinkedIn profile 